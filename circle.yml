machine:
  node:
    version: 6.2.0

dependencies:
  cache_directories:
    - "client/typings"
    - "client/node_modules"
  pre:
    - npm install -g npm
    
deployment:
  staging:
    branch: master
    heroku:
      appname: the-league-test